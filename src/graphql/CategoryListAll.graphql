query CategoryListAll($first: Int!, $after: String) {
	categories(first: $first, after: $after, sortBy: { field: NAME, direction: ASC }) {
		pageInfo {
			hasNextPage
			endCursor
		}
		edges {
			node {
				id
				name
				slug
				parent {
					id
				}
			}
		}
	}
}
